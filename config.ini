; OBS-Plugins configuration file (INI-style)
; Replace values below with your Twitch credentials and streamer id.
; Use %date% inside title templates to be replaced by current date (YYYY-MM-DD).

[Twitch]
client_id =
access_token =
streamer_id =

[Paths]
; Path to the JSON config file that contains title templates
config_json = config.json